function createTitle(bookIdea) {
  console.log("The " + bookIdea);
  return ("The " + bookIdea);
}

createTitle();

module.exports = {
  createTitle,
  // buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}
